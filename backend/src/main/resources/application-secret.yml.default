spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            clientId: NEED_TO_INPUT

          naver:
            client-id: { client-id }
            client-secret: { client-secret }

          google:
            client-id: { client-id }
            client-secret: { client-secret }
custom:
  jwt:
    secretKey: NEED_TO_INPUT